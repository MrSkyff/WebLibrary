const addTagFormReader = document.querySelector("#articleTagModalForm");

categoryFormReader.addEventListener("submit", function (event) {
    event.preventDefault();

    console.log("Event tag");
});